/*

Q4: Create a class named `Student` with `name` and `age`, and a constructor to initialize 
these values. 

*/
class Student {
  String name;
  int age;

  // Constructor to initialize name and age
  Student(this.name, this.age);
}